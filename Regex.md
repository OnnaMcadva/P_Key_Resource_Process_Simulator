
## 🔹 1. Якоря (позиции в строке)

| Regex | Значение         | Пример строки | Совпадение           |
| ----- | ---------------- | ------------- | -------------------- |
| `^`   | начало строки    | `abc`         | позиция перед `a`    |
| `$`   | конец строки     | `abc`         | позиция после `c`    |
| `\b`  | граница слова    | `abc def`     | между `c` и пробелом |
| `\B`  | не граница слова | `abc_def`     | между `c` и `_`      |

---

## 🔹 2. Символьные классы

| Regex | Значение                  | Пример        |
| ----- | ------------------------- | ------------- |
| `.`   | любой символ (кроме `\n`) | `a`, `1`, `#` |
| `\d`  | цифра                     | `0–9`         |
| `\D`  | не цифра                  | `a`, `_`      |
| `\w`  | буква, цифра, `_`         | `aZ9_`        |
| `\W`  | не `\w`                   | `@# `         |
| `\s`  | пробельный символ         | пробел, `\t`  |
| `\S`  | не пробельный             | `a`, `1`      |

---

## 🔹 3. Квадратные скобки `[]`

| Regex          | Значение             |
| -------------- | -------------------- |
| `[abc]`        | `a` или `b` или `c`  |
| `[^abc]`       | любой, кроме `a,b,c` |
| `[a-z]`        | строчная буква       |
| `[A-Z]`        | заглавная буква      |
| `[0-9]`        | цифра                |
| `[a-zA-Z0-9_]` | то же, что `\w`      |

⚠️ `^` внутри `[]` = **НЕ**, а не «начало строки»

---

## 🔹 4. Квантификаторы (сколько раз)

| Regex   | Значение       |
| ------- | -------------- |
| `*`     | 0 или больше   |
| `+`     | 1 или больше   |
| `?`     | 0 или 1        |
| `{n}`   | ровно `n`      |
| `{n,}`  | `n` или больше |
| `{n,m}` | от `n` до `m`  |

---

## 🔹 5. Жадность / ленивость

| Regex | Поведение         |
| ----- | ----------------- |
| `.*`  | жадный (максимум) |
| `.*?` | ленивый (минимум) |
| `.+?` | ленивый `+`       |

---

## 🔹 6. Группы и альтернативы

| Regex     | Значение              |                 |
| --------- | --------------------- | --------------- |
| `(abc)`   | группа, сохраняется   |                 |
| `(?:abc)` | группа без сохранения |                 |
| `a        | b`                    | `a` **или** `b` |
| `(a       | b)c`                  | `ac` или `bc`   |

---

## 🔹 7. Экранирование (ВАЖНО в Java)

| Нужно найти | Regex в Java |
| ----------- | ------------ |
| `(`         | `\\(`        |
| `)`         | `\\)`        |
| `[`         | `\\[`        |
| `]`         | `\\]`        |
| `\`         | `\\\\`       |
| `.`         | `\\.`        |

📌 Потому что:

* `\` — спецсимвол в regex
* `\` — спецсимвол в Java-строке
  👉 экранируем **дважды**

---

## 🔹 8. Ссылки на группы

| Regex           | Значение        |
| --------------- | --------------- |
| `\1`            | повтор группы 1 |
| `(\\w+)\\s+\\1` | повтор слова    |

Пример:

```
"hello hello" → match
"hello world" → no
```

---

## 🔹 9. Флаги Pattern

| Флаг               | Значение             |
| ------------------ | -------------------- |
| `CASE_INSENSITIVE` | без учёта регистра   |
| `MULTILINE`        | `^` и `$` по строкам |
| `DOTALL`           | `.` включает `\n`    |
| `UNICODE_CASE`     | Unicode-регистр      |

Пример:

```java
Pattern.compile("abc", Pattern.CASE_INSENSITIVE);
```

---

## 🔹 10. Частые готовые шаблоны

| Задача          | Regex                        |
| --------------- | ---------------------------- |
| целое число     | `^-?\\d+$`                   |
| только буквы    | `^[a-zA-Z]+$`                |
| логин           | `^[a-zA-Z0-9_]{3,16}$`       |
| IP v4           | `^(\\d{1,3}\\.){3}\\d{1,3}$` |
| дата YYYY-MM-DD | `^\\d{4}-\\d{2}-\\d{2}$`     |
